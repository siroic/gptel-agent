#+TITLE: gptel-agent — AI Development Context
#+DESCRIPTION: Reference document for AI-assisted development of gptel-agent
#+SEQ_TODO: TODO(t) AI-DO(a) AI-DOING(i) DOING FEEDBACK(f) | AI-DONE(d) DONE(D) ARCHIVED(r) CANCELED(c)

* Overview

gptel-agent is an agentic layer built on top of gptel. It provides tool
implementations, sub-agent delegation, agent definition files, and a skill
system. It extends gptel via its preset system — agents ARE presets with
tools and custom system prompts.

** Repository

- Location: =quelpa/build/gptel-agent/=
- Depends on: gptel (=quelpa/build/gptel/=)
- See [[file:../gptel/gptel-ai.org]] for gptel core context

** What gptel-agent Extends

| Component | Extension |
|-----------+-----------|
| Presets | New presets: =gptel-agent=, =gptel-plan=, custom agents |
| Tools | 20+ tools (filesystem, web, remote, introspection, delegation) |
| Tool Preview | Custom overlay-based preview and confirmation UI |
| FSM Handlers | Custom handlers for sub-agent indication (=gptel-agent-request--handlers=) |
| Mode UI | Header-line mode switching (agent ↔ plan) |

** What gptel-agent Does NOT Override

- Core LLM request mechanism (=gptel-request=)
- Backend/model selection
- API parameter handling
- Message encoding/streaming
- Conversation history management

* Architecture

** Indirect Buffer Model

Sub-agents run inside Org indirect buffers whose lifecycle and
relationships are managed exclusively by the canonical API in
=gptel-indirect-buffer.el= (gptel core).  gptel-agent code never calls
=buffer-base-buffer= directly, never walks marker arithmetic by hand,
and never pokes at the IB registry internals — every relationship
query goes through the canonical resolvers.

Lifecycle (all in =gptel-indirect-buffer.el=):

- =gptel-org-ib-insert-child= — the single parent-aware insertion API.
  Inserts a child heading before the parent's =TERMINE= terminator,
  optionally creates an IB narrowed to the new heading.  Returns a
  plist with =:heading-marker= and =:indirect-buffer= keys.
- =gptel-org-ib-create= — open an indirect buffer narrowed to that
  subtree and register a =gptel-org-ib-node= in
  =gptel-org-ib--registry=.
- =gptel-org-ib-close= — tear down the indirect buffer, remove its
  registry node, and leave the parent's structure intact.
- =gptel-org-ib-ensure-terminator= / =gptel-org-ib-streaming-marker= —
  maintain the =TERMINE= sentinel (sole terminator keyword; =RESULTS=
  and =FEEDBACK= no longer play that role).

Resolution (the only sanctioned way to query IB relationships):

- =gptel-org-ib-parent= — parent node or base buffer
- =gptel-org-ib-children= — list of child nodes
- =gptel-org-ib-base= — root base buffer (walks the parent chain)
- =gptel-org-ib-bounds= — =(start . end)= in the parent buffer
- =gptel-org-ib-point= — current point respecting narrowing
- =gptel-org-ib-absolute-position= — =(buffer . pos)= pair

Inconsistencies are fatal: =gptel-org-ib-fatal= signals =user-error=
rather than letting corrupted state propagate.  There are no
=ignore-errors= wrappers around IB operations in gptel-agent.

Design rationale and invariants live in
[[file:../gptel/gptel-indirect-buffer-ai.org][gptel-indirect-buffer-ai.org]]
(see also [[file:../gptel/gptel-ai.org::#ib-canonical-refactor]] for
the refactor history).  This document cross-references them rather
than restating the design.

** Agent = Preset + Tools + System Prompt

#+begin_example
Agent Definition File (Markdown/Org)
  → gptel-agent-read-file (parse frontmatter + body)
  → gptel-make-preset (register as gptel preset)
  → gptel--apply-preset (activate in buffer)
  → LLM receives: system prompt + tool specs + user messages
#+end_example

** Agent Definition Format

Agents are defined in Org files with a =:PROPERTIES:= drawer (preferred)
or Markdown files with YAML frontmatter (legacy).

*** Org Format (current)

#+begin_example
:PROPERTIES:
:name: my-agent
:description: Does specific things
:tools: Read Write Bash
:model: claude-sonnet-4-20250514
:max-tokens: 8192
:backend: Claude
:subagent-models: (:gatherer "Claude/claude-haiku-4-5-20251001" :researcher "Claude/claude-sonnet-4-6")
:include-reasoning: t
:reasoning-effort: high
:pre: (lambda () (require 'gptel-agent-tools-org))
:END:

System prompt text goes here as the body (org markup).

{{AGENTS}} — expanded to list of available agents
{{SKILLS}} — expanded to list of available skills
#+end_example

*** Markdown Format (legacy)

#+begin_example
---
name: my-agent
description: Does specific things
tools:
  - Read
  - Write
  - Bash
model: claude-sonnet-4-20250514
---

System prompt body in markdown.
#+end_example

*** Property Reference

| Property | Org Format | YAML Format | Purpose |
|----------+------------+-------------+---------|
| =name= | =:name: val= | =name: val= | Agent identity |
| =description= | =:description: val= | =description: val= | Brief description (mandatory) |
| =tools= | =:tools: A B C= (space-separated) | YAML list (=- A=) | Available tool names |
| =backend= | =:backend: Claude= | =backend: Claude= | LLM backend |
| =model= | =:model: name= | =model: name= | Model name |
| =max-tokens= | =:max-tokens: 8192= | =max-tokens: 8192= | Token limit |
| =include-reasoning= | =:include-reasoning: t= | =include-reasoning: true= | How to handle LLM thinking blocks (t/nil/ignore) |
| =reasoning-effort= | =:reasoning-effort: xhigh= | =reasoning-effort: xhigh= | Reasoning effort level (low/medium/high/xhigh/max) |
| =subagent-models= | =:subagent-models: (:key "backend/model" ...)= or plist values | YAML nested object | Per-sub-agent model overrides (string or plist) |
| =pre=, =post= | Elisp expression (evaluated) | Elisp expression (evaluated) | Hooks run before/after preset |
| =parents= | Elisp list of symbols (read) | Elisp list (read) | Inherit from other presets |
| =context= | =:context: A B C= (space-separated) | YAML list | Context sources |

Notes:
- In Org format, =:tools= and =:context= are space-separated strings (split automatically)
- =:subagent-models= is an Elisp plist read with =read=, e.g. =(:gatherer "Claude/claude-haiku-4-5-20251001")=
- =:subagent-models= values can be strings or plists: =(:gatherer "Claude/claude-haiku-4-5-20251001" :researcher-deep (:model "Claude/claude-opus-4-7" :reasoning-effort "xhigh" :include-reasoning t))=
- =:include-reasoning= is converted from string: "t" → t, "nil" → nil, "ignore" → 'ignore
- =:reasoning-effort= is kept as string (no conversion needed)
- =:max-tokens= is converted from string to number
- =:pre= and =:post= are Elisp expressions evaluated with =(eval (read val) t)=
- =:parents= is read as Elisp and interned to symbols
- The =CATEGORY= property (added automatically by Org) is ignored

** Two-Mode Operation

1. *Agent Mode* (=gptel-agent= preset) — full tool access, interactive
2. *Plan Mode* (=gptel-plan= preset) — read-only filesystem, planning focus

Switched via header-line buttons in agent buffers.

** Sub-Agent Delegation

- Parent agent calls =Agent= tool with: =type=, =description=, =prompt=
- Sub-agent runs in isolated context (no shared history)
- Task overlay shows progress (Waiting → Calling Tools → Complete)
- Result returned as text to parent agent
- Each sub-agent type has its own agent definition file
- *Handover mode*: when =handover=true=, the sub-agent's output creates a new =AI-DO= heading at the user task level instead of returning text to the caller.  The system navigates up past all agent headings (via =gptel-org-ib-find-user-task-heading=) to find the user-level task heading, then inserts the new =AI-DO= heading as a sibling of the top-level agent subtree.  Parent TODO content (the full user task subtree) is auto-injected into the handover agent's prompt so it can read all accumulated context including triage agent findings.  Used for escalation workflows (triage → specialist).
- *User-initiated handoff*: =gptel-org-handoff= command stops the current task and creates a new =AI-DO= sibling heading for continuation with fresh context.  Useful when a conversation has grown too large for efficient token usage.

** Org Format Instructions

The prompt transform =gptel-org-agent--transform-org-instructions= (in =gptel-org-agent.el=, part of the gptel package) appends org formatting instructions to the system message.  It resolves the heading level dynamically and tells the LLM to respond in org-mode syntax.

Two modes:

- *Legacy (tag-based)*: AI is told to use exact heading levels (e.g. =**** Heading=).  =:assistant:= / =:user:= tags mark conversation turns.
- *TODO keyword mode* (=gptel-org-use-todo-keywords=): AI is told to start with =* AI <title>= using level 1 headings.  A real-time auto-corrector (=gptel-org--auto-correct-stream= on =gptel-post-stream-hook=) rebases heading levels, escapes example blocks, and converts leftover markdown fences as the response streams in.

The legacy tag-based approach is *deprecated* in favor of TODO keyword mode.  See [[file:../gptel/gptel-ai.org::*Discussion Structure: AI/FEEDBACK Keywords]] for full details.

** Custom FSM Handlers

#+begin_src elisp
(defvar gptel-agent-request--handlers
  `((WAIT ,#'gptel-agent--indicate-wait
          ,#'gptel--handle-wait)
    (TOOL ,#'gptel-agent--indicate-tool-call
          ,#'gptel--handle-tool-use)))
#+end_src

These provide visual feedback (overlay updates) during tool execution.

* Source Files

| File | Lines | Purpose |
|------+-------+---------|
| =gptel-agent.el= | main | Agent framework: definitions, presets, parser, template expansion |
| =gptel-agent-tools.el= | main | Core tools: filesystem, web, system, delegation (Agent, Skill) |
| =gptel-agent-tools-org.el= | ext | Org-mode tools: OrgOutline, OrgHeading, ReadOrgLink |
| =gptel-agent-tools-remote.el= | ext | Remote server tools via TRAMP SSH |
| =gptel-agent-tools-introspection.el= | ext | Emacs introspection: symbol/source lookup, manual, eval |

** Tests

All tests live under =test/= and run via a Makefile that mirrors
gptel's convention:

#+begin_example
cd quelpa/build/gptel-agent/test && make test
#+end_example

| File | Purpose |
|------+---------|
| =test/gptel-agent-test.el= | Loader: =gptel-agent-parse-org-properties=, =:state-words= parsing, scaffold command |
| =test/test-bash-safety.el= | Bash safety predicates (safe / confirm / file-op checks) |
| =test/test-gptel-agent-trace.el= | =gptel-agent-trace= pure functions |

The Makefile loads every =*.el= at the repo root (source) and in
=test/=, then runs =ert-run-tests-batch-and-exit=.  gptel source must
be available at the sibling path =../../gptel=.

* Agent Definitions

Located in =agents/= subdirectory as =.org= files. Loaded via =gptel-agent-dirs=.

| Agent | Purpose | Tool Access |
|-------+---------+-------------|
| =gptel-agent= | Full agent (Opus), all tools | All |
| =gptel-triage= | Lightweight triage agent (local Ollama), gathers context and hands off to specialists | All |
| =gptel-plan= | Planning with read-only access | Read-only filesystem |
| =executor= | Autonomous multi-step task execution (Haiku) | All tools |
| =implementer= | Focused implementation: file creation/modification (Opus). Cannot delegate to itself; delegates to =verifier= on test failures. | All tools |
| =implementer-complex= | Complex multi-step implementation with broad delegation (Opus, coordinator-based). Can delegate to =implementer=; cannot delegate to itself. | All tools |
| =verifier= | Post-implementation verification and fixing: tests, compilation, lint failures (Opus). Receives synthesized focused context from main/implementer; starts with a fresh small context. Cannot delegate to =implementer= or =verifier=. | All tools |
| =researcher= | Codebase/web exploration (Sonnet) | Read-only file + web |
| =researcher-deep= | Deep analysis and debugging (Opus) | Read-only file + web |
| =gatherer= | Lightweight context lookups (Haiku) | Read-only: Glob, Grep, Read, Eval, Bash |
| =introspector= | Emacs/elisp exploration (Sonnet) | Introspection + org tools |
| =remote-server= | Remote server management via SSH (Haiku) | Remote tools |
| =archive-searcher= | Search archived AI task conversations (Haiku) | Glob, Grep, Read, Eval |
| =handover= | Handover plan writer (Opus, explicit model), reads triage context | Read-only file + org tools |

** State Triads

Each agent declares a =:state-words:= triad =(REQUEST DOING DONE)= in
its =:PROPERTIES:= drawer.  The three words are the Org TODO keywords
used for that agent's subtree across its lifecycle:

- *REQUEST* — keyword on the freshly-inserted heading (before the
  sub-agent starts running)
- *DOING* — keyword while the sub-agent is streaming; set by
  =gptel-org-agent= when the sub-agent's indirect buffer opens (IB-5.3)
- *DONE* — keyword after the sub-agent completes; rewritten by
  =gptel-agent-subtree--cleanup= (see [[*Sub-Agent Subtree Handlers]])

Non-agent sub-tasks collapse to three copies of the tool's own
keyword.  When the property is missing the default fallback is
=(PENDING RUNNING DONE)=.

The triad is read via =gptel-agent-state-words= (=gptel-agent.el=) and
consumed from three places:

- =gptel-org-agent.el= — derives the DOING keyword when opening the
  sub-agent's indirect buffer
- =gptel-org.el= — iterates all agent plists and registers every
  triad word into =org-todo-keyword-faces= so they fontify correctly
- =gptel-agent-tools.el= — reads the DONE slot during cleanup-time
  keyword rewriting

Current triads for registered built-in agents:

| Agent | REQUEST | DOING | DONE |
|-------+---------+-------+------|
| =gptel-agent= | =AGENT= | =AGENTING= | =AGENTED= |
| =gptel-triage= | =TRIAGE= | =TRIAGING= | =TRIAGED= |
| =gptel-plan= | =PLAN= | =PLANNING= | =PLANNED= |
| =executor= | =EXECUTE= | =EXECUTING= | =EXECUTED= |
| =implementer= | =IMPLEMENT= | =IMPLEMENTING= | =IMPLEMENTED= |
| =implementer-complex= | =IMPLEMENT-COMPLEX= | =IMPLEMENTING-COMPLEX= | =IMPLEMENTED-COMPLEX= |
| =verifier= | =VERIFY= | =VERIFYING= | =VERIFIED= |
| =researcher= | =RESEARCH= | =RESEARCHING= | =RESEARCHED= |
| =researcher-deep= | =DEEP-RESEARCH= | =DEEP-RESEARCHING= | =DEEP-RESEARCHED= |
| =gatherer= | =GATHER= | =GATHERING= | =GATHERED= |
| =introspector= | =INTROSPECT= | =INTROSPECTING= | =INTROSPECTED= |
| =remote-server= | =REMOTE-OP= | =REMOTE-OPING= | =REMOTE-OPED= |
| =archive-searcher= | =ARCHIVE-SEARCH= | =ARCHIVE-SEARCHING= | =ARCHIVE-SEARCHED= |
| =handover= | =HANDOVER= | =HANDING-OVER= | =HANDED-OVER= |

Additional agents shipped under =agents/= but not registered as
top-level presets:

| Agent | REQUEST | DOING | DONE |
|-------+---------+-------+------|
| =gptel-coordinator= | =COORDINATE= | =COORDINATING= | =COORDINATED= |
| =gptel-dummy= | =DUMMY= | =DUMMYING= | =DUMMIED= |

Each agent's =:PROPERTIES:= drawer is authoritative — if a triad
changes there, re-read the drawer rather than editing this table
first.  Grammar: exactly three whitespace-separated words.  See
[[file:../gptel/gptel-indirect-buffer-ai.org::#target-design][gptel-indirect-buffer-ai.org → target-design]]
for how triads interact with IB lifecycle.

** Loading Pipeline

1. =gptel-agent-dirs= specifies directories to scan
2. =gptel-agent--update-agents= discovers =.org= and =.md= files
3. =gptel-agent-read-file= dispatches on extension:
   - =.org= → =gptel-agent-parse-org-properties= (reads =:PROPERTIES:= drawer)
   - =.md= → =gptel-agent-parse-markdown-frontmatter= (reads YAML frontmatter)
4. Template expansion (={{AGENTS}}=, ={{SKILLS}}=)
5. Three top-level agents registered as presets via =gptel-make-preset=:
   =gptel-agent=, =gptel-plan=, =gptel-triage=
6. Tool enum updated for the =Agent= tool's =:enum= (excludes top-level agents)
7. Each agent's =:state-words:= triad (see [[*State Triads]]) is read via
   =gptel-agent-state-words= and its three keywords are registered in
   =org-todo-keyword-faces= (done in =gptel-org.el= at preset-apply time)

* Tool System

** Categories and Tools

*** Category: gptel-agent (core tools)

| Tool | Async | Confirm | Purpose |
|------+-------+---------+---------|
| =Read= | no | no | Read file contents with optional line range |
| =Write= | no | yes | Create/overwrite file |
| =Edit= | no | yes | Find-and-replace in file |
| =Insert= | no | yes | Insert text at line number |
| =Glob= | no | no | Find files by name pattern |
| =Grep= | no | no | Search file contents with regex |
| =Bash= | yes | yes | Execute shell commands |
| =Eval= | no | no | Evaluate elisp expression |
| =Diagnostics= | no | no | Get buffer diagnostics (flymake/flycheck) |
| =Agent= | yes | no | Delegate to sub-agent (supports =handover= param) |
| =Skill= | no | no | Load skill instructions |
| =WebSearch= | yes | no | Search the web |
| =WebFetch= | yes | no | Fetch and extract from URL |

*** Category: remote

| Tool | Async | Confirm | Purpose |
|------+-------+---------+---------|
| =RemoteBash= | yes | yes | Shell command on remote host |
| =RemoteRead= | no | no | Read remote file |
| =RemoteWrite= | no | yes | Write remote file |
| =RemoteEdit= | no | yes | Edit remote file |
| =RemoteGlob= | no | no | Find remote files |
| =RemoteGrep= | no | no | Search remote file contents |
| =ServiceStatus= | no | no | Check systemd service |
| =ServiceControl= | no | yes | Start/stop/restart service |

See [[file:../gptel/gptel-remote-ai.org]] for detailed remote tools documentation.

*** Category: introspection

| Tool | Async | Confirm | Purpose |
|------+-------+---------+---------|
| =DescribeSymbol= | no | no | Describe elisp symbol |
| =FindSource= | no | no | Find symbol source code |
| =LookupManual= | no | no | Search Emacs manual |
| =InfoNode= | no | no | Read Info documentation node |
| =ListFeatures= | no | no | List loaded Emacs features |
| =LibrarySource= | no | no | Read library source file |
| =EvalExpression= | no | no | Evaluate elisp (introspection context) |

** Preview System

Tools with side effects register preview functions via =gptel--tool-preview-alist=:
- Overlays show proposed action at point
- Syntax-highlighted content (code, diffs)
- Navigation: =n=/=p= keys, toggle with =<tab>=
- Confirm: =RET=, reject: =q=

** Async Tool Pattern

#+begin_src elisp
;; Async tools receive a callback as last argument
(defun my-async-tool (arg1 arg2 callback)
  (do-something-async arg1 arg2
    (lambda (result)
      (funcall callback (format "Result: %s" result)))))
#+end_src

** Sub-Agent Subtree Handlers

When =gptel-org-subtree-context= is enabled, sub-agents stream directly
into their own indirect buffer (opened via =gptel-org-ib-create=)
instead of accumulating output into a callback string.  The handler
table =gptel-agent-subtree--handlers= drives a custom DONE chain that
delegates all IB operations to the canonical API in
=gptel-indirect-buffer.el= (see [[*Indirect Buffer Model]]):

| Handler | Purpose |
|---------+---------|
| =gptel-agent-subtree--handle-post-insert= | Like =gptel--handle-post-insert= but omits separator/prefix insertion (sub-agents are non-interactive) |
| =gptel-agent-subtree--handle-done= | Extracts text from the sub-agent's indirect buffer (via =gptel-org-ib-bounds=), then calls parent's =main-cb= |
| =gptel-agent-subtree--cleanup= | Calls =gptel-org-ib-close= to tear down the indirect buffer, rewrites the subtree's TODO keyword from the agent's DOING word to its DONE word (see [[*State Triads]]), advances parent's corrector past the folded subtree |

* Skill System

- =gptel-agent-skill-dirs= specifies directories for skill files
- Skills are instruction documents loaded on demand via =Skill= tool
- Referenced by name in agent system prompts
- Skills provide task-specific guidance without being full agents

* Key Data Structures

** Global Variables

| Variable | Type | Purpose |
|----------+------+---------|
| =gptel-agent--agents= | alist | Known agents: =(name . plist)= |
| =gptel-agent--skills= | alist | Known skills: =(name . (dir . plist))= |

** Customization

| Variable | Type | Purpose |
|----------+------+---------|
| =gptel-agent-dirs= | (repeat directory) | Where to find agent definitions |
| =gptel-agent-skill-dirs= | (repeat directory) | Where to find skill definitions |

** Agent Plist Format

#+begin_src elisp
(:name "agent-name"
 :description "Brief description"
 :tools ("tool1" "tool2")         ; list of tool name strings
 :system "System prompt text"
 :backend "Claude"                ; backend name string (resolved to struct on apply)
 :model "model-name"
 :max-tokens 8192
 :include-reasoning t
 :reasoning-effort "xhigh"
 :subagent-models (:gatherer "Claude/claude-haiku-4-5-20251001"
                   :researcher-deep (:model "Claude/claude-opus-4-7"
                                     :reasoning-effort "xhigh"
                                     :include-reasoning t))
 :pre (lambda () ...)
 :post (lambda () ...))
#+end_src

=:subagent-models= maps sub-agent type keywords to model specifications.
Values can be:
- A string like ="Claude/claude-opus-4-7"= (just model override)
- A plist like =(:model "Claude/claude-opus-4-7" :reasoning-effort "xhigh" :include-reasoning t)= (model + extra params)

Extra params from plist values (=:reasoning-effort=, =:include-reasoning=, etc.) are merged into the sub-agent's preset at layer 2 (parent's subagent-models), between the defaults (layer 1) and the agent's own properties (layer 3).

Used by =gptel-agent--get-parent-subagent-model= to override settings when a parent agent spawns sub-agents.

=:agent-handover= is a runtime-only boolean set on the FSM info plist when a
sub-agent is spawned with =handover=true=.  See [[*Tool Call Info Plist (FSM context)]].

** Tool Call Info Plist (FSM context)

#+begin_src elisp
(:position POINT
 :context OVERLAY           ; agent task overlay
 :tool-use [calls...]
 :tracking-marker MARKER
 :error ERROR-DATA
 :transformer FUNC          ; result transformer
 :post (callbacks...)
 :agent-handover BOOL       ; when t, sub-agent uses handover completion flow
 :agent-type STRING         ; sub-agent type name
 :agent-description STRING) ; sub-agent task description
#+end_src

* Error Handling

| Scenario | Mechanism | Recovery |
|----------+-----------+---------|
| File not readable | =error= signal | Caught by gptel, shown as message |
| Tool timeout | =run-at-time= + abort | Callback notifies LLM of failure |
| Process exit non-zero | Exit code check | Error with stderr returned to LLM |
| Malformed frontmatter | =error= in parser | Agent file ignored |
| User abort (=C-c C-c=) | =gptel-abort-hook= | Overlays removed, FSM aborted |
| Missing tool | Excluded from list | Agent works with remaining tools |

* Feature-Specific Documents

| Document | Feature | Status |
|----------+---------+--------|
| [[file:../gptel/gptel-remote-ai.org]] | Remote server tools | exists |
| [[file:../gptel/gptel-archive-ai.org]] | Archive searcher agent context | exists |
| [[file:../gptel/gptel-ai.org]] | gptel core (parent package) | exists |
| [[file:../gptel/gptel-ai.org::*Discussion Structure: AI/FEEDBACK Keywords]] | AI/FEEDBACK keyword mode, auto-corrector, FEEDBACK cycle | exists |
| [[file:../gptel/gptel-indirect-buffer-ai.org]] | Canonical indirect-buffer API (lifecycle, registry, resolvers, TERMINE terminator) | exists |

* TODOs
** Adhoc TODOs

*** AI-DONE DeepSeek tool launch nil-args root cause fix
**** Origin
- DeepSeek reasoning models emit tool calls where :arguments can be nil/null
- gptel-openai.el wraps json-parse-string in ignore-errors, silently swallowing the wrong-type-argument stringp nil error 
- The nil :args propagates through gptel--map-tool-args → tool function receives nil args → crashes at file-readable-p
- The existing AI-DONE "Fix nil-args crash" only fixed the preview path; this fixes the root cause in gptel-openai.el and adds execution-path defenses
**** Discussion highlights
- Root cause: ignore-errors swallows json-parse-string failure when :arguments is nil
- Affects both streaming (line 122) and non-streaming (line 212) paths in gptel-openai.el
- Secondary: gptel-agent--edit-file calls file-readable-p without nil guard — crashes before the existing stringp check
- Fixes: guard :arguments before json-parse-string, default nil :args to empty plist, add input validation to --edit-file
**** Mission
Implemented in this task — mark as AI-DONE after verification

**** Outcomes / Results
- gptel repo (commit 6dbb5b1): Guarded :arguments with stringp check before json-parse-string in both streaming and non-streaming paths; default nil :args to empty plist in gptel--handle-tool-use; added warning log when args is nil
- gptel-agent repo (commit 392b565): Added stringp guard for path/old-str/new-str as first check in --edit-file; added regression test (4 nil-arg cases); gptel-agent tests 43/43 passing
- gptel tests: 480/484 (1 pre-existing unrelated failure in gptel-org-agent-respond-text-no-same-line-terminator)
*** AI-DONE Fix nil-args crash in =gptel-agent--edit-files-preview-setup=
**** Origin
- User hit a hard crash mid-conversation when the model issued an =Edit=
  tool call with only =path= and no =old_str=, =new_str=, or =diff=:
  #+begin_src
    Debugger entered--Lisp error: (wrong-type-argument stringp nil)
      gptel-agent--edit-files-preview-setup
        (("quelpa/build/gptel/test/gptel-org-agent-test.el" nil nil nil) ...)
  #+end_src
- The crash happens in the tool-confirmation preview (before the user even
  approves the call), so the whole =gptel--display-tool-calls= path aborts
  via =gptel-curl--sentinel=. The conversation is left in a broken state
  with no chance to reject or repair the malformed call.

**** Discussion highlights
- =gptel-agent--edit-files-preview-setup= ([[file:gptel-agent-tools.el::731]])
  destructures =arg-values= as =(path old-str new-str-or-diff diffp)= and
  has *two* render branches:
  - Patch branch: =(if (and diffp (not (eq diffp :json-false))) ...)=
  - Text-replacement branch: =(when old-str ...)=
- With args =(path nil nil nil)= *both branches are skipped*, so the
  local =description= is never bound. The crash site is:
  #+begin_src elisp
    (insert
     "(" (propertize description 'font-lock-face 'font-lock-keyword-face)
     ...)
  #+end_src
  =propertize= requires a string; =description= is nil → =stringp nil=.
- Adjacent latent bug: had =old-str= been a string but =new-str-or-diff=
  nil, the failure would shift to =(propertize new-str-or-diff ...)=
  inside the text-replacement branch (line ~759).
- Tool spec at lines 2272–2316 of =gptel-agent-tools.el=: =new_str= and
  =diff= are *not* marked =:optional t=, so the model is expected to
  provide them. But gptel's argument parser fills missing JSON args with
  nil rather than raising, so the preview function /must/ defend itself.
- The preview function is dispatched generically through
  =gptel--tool-preview-alist= (registered at lines 2080–2089). Other
  preview functions in the same file (=write-file=, =insert-in-file=,
  =bash=, =eval-elisp=) should be audited for the same nil-arg pattern.

**** Decisions
Pick one (default: *A + C*):
- *Option A — Defensive preview-setup (minimum viable fix).* Make
  =gptel-agent--edit-files-preview-setup= produce a sensible preview
  even with malformed args: detect the all-nil-edit-args case, set
  =description= to ="Edit?"= (or similar marker) and =files-affected=
  to =(list path)=, and skip the body entirely. The user then sees a
  confirmation overlay clearly labelled as malformed and can reject it.
  - Pros: surgical, contains the failure to one tool, no flow changes.
  - Cons: doesn't help other preview functions if they share the bug.
- *Option B — Validate tool args before invoking preview.* Add a
  pre-check in the =gptel-org-agent--display-tool-calls-advice=
  wrapper (or upstream of it) that, for each tool call, verifies all
  *required* args (per the tool's =:args= spec, ignoring =:optional t=)
  are non-nil; on failure, short-circuit by returning an error result
  to the model so it can retry, *without* invoking the preview at all.
  - Pros: fixes an entire class of bugs (any preview that assumes
    required args are non-nil); produces a useful error the model can
    act on; doesn't crash mid-confirmation.
  - Cons: larger surface, touches the advice/dispatch layer; needs a
    decision on whether to surface the error to the user or just to
    the model.
- *Option C — Audit all preview functions for nil-arg defensiveness.*
  Sweep =gptel-agent-tools.el= preview-setup functions
  (=write-file=, =insert-in-file=, =bash=, =eval-elisp=, =task=) and
  ensure each handles missing required args without crashing.
  - Pros: hardens the whole tool-preview path.
  - Cons: pure cleanup work; no single user-visible bug drives it.
- *Recommended:* *A + C* now (small, targeted, prevents recurrence in
  other tools); *B* deferred as follow-up if the same failure mode is
  observed for other tools or if we want the model to see the error.

**** Mission
Implement *Option A + C* in =quelpa/build/gptel-agent/gptel-agent-tools.el=:

1. Patch =gptel-agent--edit-files-preview-setup= ([[file:gptel-agent-tools.el::731]]):
   - After destructuring =arg-values=, detect the malformed case
     (=diffp= falsy/=:json-false= AND =old-str= nil). In that case:
     - Set =description= to ="Edit?"=.
     - Set =files-affected= to =(list (or path "<no path>"))=.
     - Insert a clearly-marked placeholder body, e.g.
       =(propertize "<malformed Edit call: missing diff/old_str/new_str>"
       'font-lock-face 'font-lock-warning-face)= followed by a newline.
     - Skip the existing patch and text-replacement bodies.
   - Inside the text-replacement branch, also guard against
     =new-str-or-diff= being nil: substitute ="<empty>"= or skip
     =propertize= for that piece.

2. Audit and harden the sibling preview-setup functions in the same
   file. For each, identify which args are dereferenced as strings
   without a nil guard, and add =or= fallbacks (e.g. =(or path "")=)
   or early-return placeholders mirroring step 1:
   - =gptel-agent--write-file-preview-setup=
   - =gptel-agent--insert-in-file-preview-setup=
   - =gptel-agent--execute-bash-preview-setup=
   - =gptel-agent--eval-elisp-preview-setup=
   - =gptel-agent--task-preview-setup=
   (Auditor should report which actually had the issue and what was
   changed; functions already nil-safe need no edit.)

3. Add a regression test under
   =quelpa/build/gptel/test/= (or the gptel-agent tests, wherever the
   existing preview tests live — verify by searching for
   =edit-files-preview-setup= or =write-file-preview-setup= in the
   test suite). The test should call
   =gptel-agent--edit-files-preview-setup= with
   =arg-values= = =("foo.el" nil nil nil)= in a temp buffer and assert
   that no error is raised and the buffer contains the malformed
   marker text.

4. Run the gptel test suite to make sure nothing else regressed:
   =cd quelpa/build/gptel/test && make test 2>&1 | tail -30=.

5. Commit with a message like:
   #+begin_src
     fix: defensive preview-setup against nil tool args

     gptel-agent--edit-files-preview-setup crashed with
     (wrong-type-argument stringp nil) when the model issued an Edit call
     with only =path=. Both render branches were skipped, leaving
     =description= nil for a downstream =propertize=.

     Show a clearly-marked malformed-call placeholder instead, and apply
     the same nil-guard pattern across the other preview-setup functions.
   #+end_src

**** Outcomes / Results / Follow-up linked tasks
- Implemented *Option A + C* in
  =quelpa/build/gptel-agent/gptel-agent-tools.el=:
  - =gptel-agent--edit-files-preview-setup=: refactored =if=/=when= into
    =cond= with three branches (Patch / ReplaceIn / malformed). Patch
    branch now requires =new-str-or-diff= to be a string; ReplaceIn
    branch requires =old-str= a string and substitutes ="<missing
    new_str>"= for nil =new-str-or-diff=; malformed branch sets
    =description= to ="Edit?"= and inserts a warning-faced placeholder.
  - =gptel-agent--eval-elisp-preview-setup=: substitutes
    ="<missing expression>"= when =expr= is nil.
  - =gptel-agent--execute-bash-preview-setup=: substitutes
    ="<missing command>"= when =command= is nil.
  - =gptel-agent--write-file-preview-setup=: substitutes warning-faced
    ="<missing content>"= when =content= is nil; defends
    =gptel-agent--fontify-block= with =(or filename "")=.
  - =gptel-agent--insert-in-file-preview-setup=: shows ="<no path>"=
    label when =path= is nil; falls through to the [File not readable]
    branch unless all required args are valid.
  - =gptel-agent--task-preview-setup=: substitutes ="<no type>"=,
    ="<no description>"=, ="<no prompt>"= for nil args.
- Added regression tests in
  =quelpa/build/gptel-agent/test/gptel-agent-test.el= covering all six
  preview functions: =gptel-agent-edit-files-preview-setup-handles-nil-args=,
  =-write-file-=, =-eval-elisp-=, =-execute-bash-=,
  =-insert-in-file-=, =-task-=. Helper
  =gptel-agent-test--call-preview= invokes each preview in a temp
  buffer.
- Test suite: 41/41 passing
  (=cd quelpa/build/gptel-agent/test && make test=).
- Option B (advice-layer arg validation) deferred as follow-up; not
  yet justified — the defensive previews already prevent the crash and
  let the user see/reject the malformed call.

*** TODO Allow per-buffer override of subagent-models via org property
**** Origin
- User reports that =:SUBAGENT-MODELS:= in a chat buffer's PROPERTIES
  drawer does not override the active =GPTEL_PRESET=. Example:
  #+begin_src org
    ,:PROPERTIES:
    :GPTEL_PRESET: gptel-agent
    :SUBAGENT-MODELS: (:gatherer "DeepSeek/deepseek-v4-flash" :executor "DeepSeek/deepseek-v4-flash" ...)
    ,:END:
  #+end_src
- Cross-ref: [[file:../gptel/gptel-ai.org::*Document GPTEL_ prefix requirement]]
  covers the related =BACKEND=/=MODEL= naming issue. This TODO is the
  *real bug*: =subagent-models= has no per-buffer override path at all,
  even with a corrected property name.

**** Discussion highlights
- =gptel--apply-preset= ([[file:../gptel/gptel.el::3319]]) explicitly
  skips =:subagent-models= when expanding presets:
  #+begin_src elisp
    ((or :parents :description :pre :post :subagent-models) nil)
  #+end_src
  So even if a chat-buffer preset carried =:subagent-models=, applying
  it would not bind anything for downstream code to see.
- =gptel-agent--get-parent-subagent-model=
  ([[file:gptel-agent-tools.el::1824]]) reads only from the file-parsed
  =gptel-agent--agents= alist, keyed by =(symbol-name gptel--preset)=:
  #+begin_src elisp
    (parent-plist (cdr (assoc parent-name gptel-agent--agents)))
    (subagent-models (plist-get parent-plist :subagent-models))
  #+end_src
  No buffer-local state, no chat-buffer property, is consulted.
- =gptel-org--entry-properties= ([[file:../gptel/gptel-org.el::1109]])
  has no =GPTEL_SUBAGENT_MODELS= entry, so the property would not even
  be read off the buffer.
- Net: there is no path by which a user can override =subagent-models=
  per buffer or per subtree. The only way to change them today is to
  edit the agent definition file and reload =gptel-agent--agents=.

**** Decisions
Pick one (default: A):
- *Option A — Buffer-local override (recommended).* Minimal, localised:
  1. Add =GPTEL_SUBAGENT_MODELS= to =gptel-org--entry-properties=
     property list.
  2. In =gptel-org--restore-state= and =gptel-org--send-with-props=,
     =read= the property value and bind a buffer-local variable
     =gptel-agent-subagent-models-override= (plist).
  3. In =gptel-agent--get-parent-subagent-model=, consult the override
     first; fall back to =gptel-agent--agents= as today.
  - Pros: surgical change, doesn't touch core preset machinery, no
    global mutation.
  - Cons: introduces gptel-agent awareness in gptel-org property
    list (already true for =GPTEL_TOOLS=, so precedent exists).
- *Option B — Mutate =gptel-agent--agents= per buffer.* Apply the
  property by patching the alist entry for the active preset on
  buffer setup. Simpler at the lookup site but mutates global state
  and is brittle when multiple buffers use the same preset
  concurrently. *Not recommended.*
- *Option C — Make gptel core honour =:subagent-models= in presets.*
  Remove =:subagent-models= from the skip list in
  =gptel--apply-preset=, set a buffer-local variable, and have
  gptel-agent read that variable. Most general (works for any preset
  source), but expands gptel core surface and only benefits
  gptel-agent. *Defer unless a second consumer appears.*

**** Mission (assumes Option A)
1. *gptel side* (=quelpa/build/gptel/gptel-org.el=):
   - Add =GPTEL_SUBAGENT_MODELS= to the property list at line 1109.
   - In =gptel-org--send-with-props=: parse the property string with
     =read=, set buffer-local =gptel-agent-subagent-models-override=
     for the duration of the send (let-bind alongside other
     overrides).
   - In =gptel-org--restore-state= (line ~1444): same parse+bind on
     buffer open.
   - Defensive: catch =read= errors and warn rather than aborting.
2. *gptel-agent side* (=quelpa/build/gptel-agent/gptel-agent-tools.el=):
   - Define =defvar-local gptel-agent-subagent-models-override nil=.
   - In =gptel-agent--get-parent-subagent-model= (line ~1824), check
     the override first:
     #+begin_src elisp
       (subagent-models
        (or gptel-agent-subagent-models-override
            (plist-get parent-plist :subagent-models)))
     #+end_src
3. *Tests*:
   - Add a test that sets =GPTEL_SUBAGENT_MODELS= on a chat buffer
     and verifies =gptel-agent--get-parent-subagent-model= returns
     the overridden value, falling back to the preset default for
     unspecified agent types.
   - Run =cd quelpa/build/gptel/test && make test 2>&1 | tail -30=.
4. *Docs*: update =gptel-agent-ai.org= subagent-models section to
   document the new property. Cross-link from
   [[file:../gptel/gptel-ai.org::*Document GPTEL_ prefix requirement]].

**** Follow-ups (capture if discovered during work)
- Consider whether =GPTEL_SUBAGENT_MODELS= should be inheritable
  per-subtree (org property inheritance) the way other =GPTEL_*=
  properties already are. Likely yes — confirm the property-list
  read path already uses =org-entry-get ... 'selective= or
  inheritance flag and behaves consistently.

*** TODO Support Siro-AI methodology auto-transitions and commits
- Origin :: Referenced from [[file:~/siro-ai/siro-ai.org::*Methodology needs gptel-agent support for auto state transitions and commits][siro-ai.org → Adhoc TODO]]. The
  Siro-AI methodology Process (steps 3 and 6) requires gptel-agent
  to automate git operations and cross-doc TODO state transitions
  so the workflow runs end-to-end without manual git commands.
- Mission :: Research and spec the required tools and hooks. Split
  into sub-tasks as needed:
  1. *Git tools* — confirm whether ~git-fetch~, ~git-status~,
     ~git-commit-file~, ~git-push~ already exist in
     =gptel-agent-tools.el= (or =gptel-agent-tools-org.el=). If they
     do, reuse them; if not, add them with the same safety
     conventions as the existing bash-tool predicates (no
     force-push, explicit path confirm for commits, confirm on
     destructive ops).
  2. *Cross-file TODO link resolution* — given a workspace-doc TODO
     that links to a project-doc TODO, resolve the target heading
     reliably. Use the standard ~file:...::*Heading~ form. Handle
     missing targets gracefully.
  3. *Pre-send hook* — on ~gptel-send~, fetch upstream, check for
     conflicts on the project doc containing the referenced TODO,
     flip the referenced project-doc TODO to ~AI-DOING~, commit,
     push. Abort send with a clear message on unresolvable
     conflicts.
  4. *Post-send / on-finish hook* — on transition to ~AI-DONE~,
     update the project doc, commit, and push. Capture any
     follow-up TODOs AI created during the session and ensure they
     land in the project doc.
  5. *State-change observer* — detect user transitions
     ~AI-DO~ → ~TODO~ and ~AI-DONE~ → ~DONE~ in the project doc at
     ~gptel-send~ time; auto-commit these trivial promotions if the
     user has not committed them yet.
- Key questions ::
  - Should auto-commit trigger on ~gptel-send~, or on a diff between
    send-start and send-end buffer snapshots?
  - Does gptel-agent need a new "siro-ai" mode/preset that enables
    these hooks only in siro-ai workspaces?
  - How do we avoid commit noise — batching related state changes,
    or one commit per transition?
- Dependencies :: May touch =gptel-org-agent.el= (pre/post-send
  hooks, state observation) and =gptel-agent-tools.el= (new or
  reused git tools). No external deps required.
- References :: [[file:~/siro-ai/siro-ai.org]],
  [[file:~/siro-ai/future_of_work.org]].
- Expected results :: A research spec detailed enough that it can be
  split into concrete implementer sub-tasks (one per
  hook/tool/observer), with clear acceptance criteria per sub-task.

** Researched TODOs
*** DONE Variable rename: gptel-org-agent-subtrees → gptel-org-subtree-context
- Renamed all 8 occurrences across =gptel-agent-tools.el= (7) and =gptel-agent-control.el= (1)
- The gptel package had already renamed this variable; gptel-agent was using the stale name
- This was causing =bound-and-true-p= checks to always return nil, silently disabling agent subtree features

** Improvement TODOs
*** TODO Fix first-launch byte-compile failures in gptel-agent
When =elpa/gptel-agent*= is removed and Emacs is relaunched, quelpa
rebuilds the package and byte-compilation fails — no =.elc= files are
produced. On second launch Emacs loads the =.el= files directly so the
errors go away, masking the underlying bugs. See
[[file:~/.emacs.d/debug-emacs-launch-error.org]] for the full log and
analysis.

Two blocking bugs in this repo abort compilation:

**** Bug 1: Wrong =display-buffer= action format
- File: =gptel-agent-control.el=, line 82 (=defcustom
  gptel-agent-control-display-action=)
- Default value uses flat =(FUNC . ALIST)= form; byte-compiler parses the
  unquoted function symbol at car as a call, producing:
  - =Warning: 'display-buffer-in-side-window' called with 3 arguments=
  - =Error (bytecomp): Wrong type argument: sequencep, bottom= (hard)
- Root cause: wrong format. Compare =gptel.el:1425= which uses the
  correct ='((display-buffer-in-side-window) ...)= form.
- Fix:
  #+begin_src elisp
  '((display-buffer-in-side-window)
    (side . bottom)
    (slot . 0)
    (window-height . 0.3))
  #+end_src

**** Bug 2: =provided-mode-derived-p= bare symbol (Emacs 30 convention)
- File: =gptel-agent-tools.el=, line 984
- Call:
  #+begin_src elisp
  (provided-mode-derived-p (buffer-local-value 'major-mode cb) 'org-mode)
  #+end_src
- Error: =Wrong type argument: sequencep, major-mode=
- New 2-arg convention needs =MODES= as a list.
- Fix: replace ='org-mode= with ='(org-mode)=.

**** Cascade errors (no separate fix needed)
After Bug 1 aborts compilation of =gptel-agent-control.el=, the
compiler emits spurious fallout:
- =reference to free variable 'cl-lib' / 'gptel' / 'funcall' / 'function'=
- =Symbol's value as variable is void: cl-lib=
- 2× =file has no 'lexical-binding' directive=
All 7 =gptel-agent/*.el= files actually have =lexical-binding: t= on
line 1. These warnings disappear once Bug 1 is fixed.

**** Minor warnings (optional cleanup)
- =org-element-property= not known to be defined
  (=gptel-agent-tools-org.el=): add =(declare-function
  org-element-property "org-element")=.
- =gptel-org--debug= not defined (=gptel-agent-trace.el=): benign, all
  call sites are =fboundp=-guarded.

**** Verification plan
After applying fixes:
1. =rm -rf ~/.emacs.d/elpa/gptel-agent-*=
2. Relaunch Emacs; inspect =*Warnings*= buffer.
3. =ls ~/.emacs.d/elpa/gptel-agent-*/*.elc= should list all 7 files.
4. =cd test && make test= still passes.
5. Commit atomically in this repo.

*** TODO Advisor agent for context management
- An Opus-tier agent that can read and edit the coordinator's conversation buffer
- Purpose: optimize weak-model context by summarizing/cleaning assistant messages
- Requires new infrastructure:
  - A hook in the gptel FSM between turns (post-response, pre-next-request)
  - Buffer parsing/editing functions for safe org conversation modification
  - Marking which parts the advisor can touch (assistant-only, not user/system)
  - Handling of tool call/result blocks embedded in assistant responses
- Could also be enabled for other weak agents (researcher on local models)
- Consider simpler alternatives first:
  - Sliding-window =:pre= hook that summarizes history into system message prefix
  - =gptel--num-messages-to-send= to limit context (already supported, zero effort)
*** TODO Test gptel-triage with local models
- Verify tool-calling reliability with qwen3.5:27b (hippo_smart)
- Test that subagent-models resolution works correctly
*** AI-DONE Improve implementer prompt to reduce context waste
**** Outcomes / Results
- Rewrote =agents/implementer.org= to match or exceed main agent delegation aggressiveness
- Removed duplicate "Org links → gatherer" and "Context sizing for org links" sections
- Made "Handle inline" MORE restrictive (only trivially small single-line edits or when delegation overhead exceeds savings)
- Added "Default posture: NO — delegate it" mindset paragraph
- Added missing key signals: mutating git commands → executor
- Added missing pattern matches: "I need to understand..." → researcher, "have we worked on X before" → archive-searcher, remote server patterns
- Added all 5 key principles (gatherer vs researcher vs researcher-deep, executor vs verifier, implementer vs verifier, archive-searcher, remote-server)
- Strengthened MANDATORY delegation scenarios with bolded git commit entry
- Added explicit "Git commits are ALWAYS delegated to executor, never inline" with cost justification
- Commit: =3685457= "Make implementer delegate as aggressively as main agent"
**** Origin
- User request: "Improve implementer agent to be token efficient"
- Focus: the implementer is Opus-tier and should avoid reading large content when the task is already well researched.

**** Discussion highlights
- Current =agents/implementer.org= already contains scout-first, targeted-read, and no-full-file-content guidance.
- The main remaining gap was parity with the coordinator prompt for org-link handling.
- Added explicit guidance to pass org links to =gatherer= and size =ReadOrgLink= context based on target type.
- Added explicit self-cost reminder that =implementer= itself is an expensive Opus-tier agent.
- Existing broader context-management TODO (advisor agent) remains valid but is a separate architectural effort.

**** Decisions
- Keep the solution prompt-level and incremental instead of adding new infrastructure.
- Prefer better delegation instructions before building advisor/summarizer machinery.

**** Mission
- Maintain and refine =quelpa/build/gptel-agent/agents/implementer.org= so implementer tasks stay narrowly scoped.
- If future drift appears between =agents/gptel-agent.org= and =agents/implementer.org=, keep the implementer prompt aligned on context-efficiency rules.
- Consider a future follow-up only if prompt guidance proves insufficient in practice.
- Check if local models can parse tool results and generate correct tool calls
- If tool calling is unreliable, the coordinator approach needs rethinking

*** AI-DONE Improve Insert, Edit, Write tool definitions for LLM reliability
**** Origin
- User report: "At least deepseek have challenges to make correct tool
  request — Sometimes missing path, diff, etc."
- Implementer agent now uses DeepSeek v4 Pro (commit =88deb33=).
  DeepSeek models appear more prone to parameter confusion with these
  three file-modification tools.
- Same issue noted in =*** TODO Test gptel-triage with local models=
  (line 888): "If tool calling is unreliable, the coordinator approach
  needs rethinking."

**** Research findings

*Bug: Insert function docstring contradicts tool description*

Tool description (line 2336): =Insert =new_str= after =line_number= in
file at =path= — correct behavior: insert AFTER line N.

Function docstring (line 1015): =N > 1 inserts before line N= — WRONG
on two counts:
- Wrong direction: says "before" instead of "after"
- Wrong range: =N > 1= excludes =N = 1=, but code accepts any positive N

Actual code behavior (line 1025-1029):
#+begin_src elisp
(pcase line-number
  (0 (goto-char (point-min)))       ; insert at beginning
  (-1 (goto-char (point-max)))      ; append to end
  (_ (goto-char (point-min))
     (forward-line line-number)))   ; insert AFTER line N
#+end_src

=forward-line N= from point-min lands at beginning of line N+1,
so insertion is AFTER line N. The tool description is correct;
the function docstring is wrong.

*Design issue: Write tool =path= + =filename= split is LLM-unfriendly*

Current signature (line 2357-2370):
- =path= — "The directory where to create the file"
- =filename= — "The name of the file to create"

Problems:
- LLMs naturally think of "file path" as a single entity
- Models frequently put the full path in one parameter, leaving the
  other empty or duplicating
- =expand-file-name= already handles absolute =filename= gracefully
  (ignores =path=), but this isn't documented and LLMs can't rely on it
- The =:confirm t= flag requires user approval, but a garbled path
  from parameter confusion may not be obvious in preview

*Design issue: Edit tool dual-mode parameter ambiguity*

Current signature (line 2289-2332):
- =path= (required) — file or directory
- =old_str= (optional) — text to find and replace
- =new_str= (required) — replacement text or unified diff
- =diff= (optional boolean) — =true= for diff mode, =false= otherwise

The mode-dispatch logic (line 857):
#+begin_src elisp
(if (or (eq diffp :json-false) old-str)
    ;; String replacement mode
  ;; Diff mode
#+end_src

Problems:
- When LLM provides =path= + =new_str= but omits BOTH =old_str= AND
  =diff=, code falls into diff mode and tries to apply =new_str= as a
  patch → confusing error from =patch= command
- =old_str= is =:optional t= but =new_str= is not, yet in diff mode
  =old_str= should be ignored — this asymmetry is confusing
- Boolean =diff= parameter with =:json-false= semantics adds complexity
  — LLMs may set =diff: false= meaning "I want string mode" but then
  omit =old_str= (thinking it's a write-like operation)
- The description is long (25 lines) and describes two modes — simpler
  models may not parse it correctly
- DeepSeek specifically observed to omit =path= or =diff= parameters

*Design issue: No =required= field in arg specs*

The arg spec format supports =:optional t= but has no =:required=
counterpart. Making required-ness explicit in the spec (rather than
inferred from absence of =:optional=) could help LLMs understand
which parameters must always be present.

**** Decisions
- Option A (minimal): Fix Insert docstring only.  Quick win, zero risk.
- Option B (moderate): Fix docstring + rewrite tool descriptions to be
  more LLM-friendly (shorter, clearer, with examples of common mistakes).
  Also add =:required t= to arg specs for clarity.
- Option C (structural): Option B + redesign Write tool to accept a
  single =file-path= parameter (with backward compat shim).  Also
  redesign Edit tool to use explicit =mode= enum (= "string" | "diff"=)
  instead of boolean =diff= flag.
- Option D (radical): Option C + split Edit into two separate tools
  (=Edit= for string replacement, =Patch= for diff application).  This
  eliminates mode ambiguity entirely but increases tool count.

*Leaning: Option C* — single =file-path= for Write is the most impactful
fix for DeepSeek.  Explicit =mode= for Edit reduces ambiguity without
adding tool count.  Both are backward-compatible via shims.

***** FEEDBACK Option D sounds to be best solution                :petri:

**** Mission
- [X] Fix =gptel-agent--insert-in-file= docstring: =N >= 1 inserts after line N=
- [X] Redesign Write tool: accept single =file-path= parameter
- [X] Redesign Edit tool: split into separate =Edit= (string replace)
  and =Patch= (diff apply) tools — eliminates mode ambiguity entirely
- [X] Run tests: =cd quelpa/build/gptel-agent/test && make test 2>&1 | tail -30=

**** Outcomes / Results (Option D — split tools)
Implemented *Option D* in =quelpa/build/gptel-agent/gptel-agent-tools.el=:

- *Insert docstring fix:* corrected =gptel-agent--insert-in-file=
  docstring (=N >= 1 inserts after line N=) and the inline comment in
  the =pcase= branch.  No tool description change needed (already
  said "after =line_number=").
- *Write redesigned* to take a single =file_path= argument plus
  =content=.  =gptel-agent--write-file= now expands the directory
  from =file-path= and creates parent directories on demand.  Preview
  setup and the =gptel-make-tool= spec updated to match.
- *Edit split into two tools:*
  - =Edit= (=gptel-agent--edit-file=) — pure string replacement on a
    single file.  Args: =path=, =old_str=, =new_str=. Errors clearly
    if =old_str= matches multiple times or is missing, suggesting
    =Patch= as the alternative.
  - =Patch= (=gptel-agent--patch-file=) — applies a unified diff via
    the =patch= command, on either a single file or directory.
    Args: =path=, =diff=.  Handles fenced =diff/=patch blocks and
    rewrites hunk headers via =gptel-agent--fix-patch-headers=.
  - The old four-arg =gptel-agent--edit-files= (with the boolean
    =diffp= flag and overloaded =new-str-or-diff=) is gone, removing
    the mode-ambiguity that confused DeepSeek.
- *Preview setup* split into
  =gptel-agent--edit-file-preview-setup= (string-replace diff render)
  and =gptel-agent--patch-file-preview-setup= (diff-mode render with
  affected-files extraction).  Preview alist updated with both
  entries.
- *Tests:* the existing nil-args defensiveness test was replaced by
  three new tests — =gptel-agent-edit-file-preview-setup-handles-nil-args=,
  =gptel-agent-patch-file-preview-setup-handles-nil-args=,
  =gptel-agent-write-file-preview-setup-handles-nil-args=. All 42
  tests pass.
- *=:required t= not added.* On review, the JSON schema generated by
  =gptel-make-tool= already treats every arg without =:optional t= as
  required, so the explicit marker would be redundant.  The optional
  =sudo= arg of =Bash= remains the only non-required arg in the
  affected tool surface.

**** Follow-ups
- Monitor whether DeepSeek v4 Pro implementer still produces malformed
  tool calls after these changes
- If tool-calling remains unreliable with DeepSeek, escalate to
  coordinator prompt overrides or per-model tool description variants
- Consider similar audit of =Bash=, =Glob=, =Grep= tool descriptions
*** TODO De-duplicate bash-safety test fixtures
After the test layout consolidation (=test/Makefile=, commit =6a9ac08=),
=test/test-bash-safety.el= still carries a copy of the bash safety
predicates and regex variables from =gptel-agent-tools.el=
(=gptel-agent--bash-safe-patterns=, =gptel-agent--bash-file-op-patterns=,
=gptel-agent--bash-safe-command-p=, =gptel-agent--bash-confirm-p=,
=gptel-agent--check-bash-file-ops=).

Originally duplicated to avoid =gptel= / =transient= load overhead
during isolated runs. The new Makefile loads the whole package anyway,
so the duplication is now pure drift risk — if the real predicates in
=gptel-agent-tools.el= change, the tests will keep passing against the
stale copies.

Cleanup task: delete the =;;;; ---- Duplicated definitions ...= block
in =test/test-bash-safety.el= and rely on =(require 'gptel-agent-tools)=
(or the =find=-based Makefile auto-load) for the real definitions.
Verify =make test= still passes.

*** TODO Remove stub defvars in test-gptel-agent-trace.el
=test/test-gptel-agent-trace.el= currently stubs external variables
that =gptel-agent-trace.el= declares with =defvar=
(e.g. =gptel-org-agent-tool-confirm-keywords=) so the trace module
could be loaded in isolation. With the new Makefile loading the full
package, those stubs should no longer be necessary. Remove them and
confirm =make test= still passes; if a real binding is needed, pull it
from the proper defining module instead of stubbing.

*** AI-DONE Create implementer-complex agent (Opus, coordinator-based)
**** Origin
- User request: Study possibility of =implementer-complex= that uses the
  best/most expensive model (Opus), derived from the main coordinator
  prompt rather than the current implementer prompt.
- Current =implementer= will remain on DeepSeek v4 Pro for routine tasks;
  =implementer-complex= is for when user explicitly wants maximum quality.

**** Discussion highlights / Study results
- =gptel-coordinator.org= (315 lines) has the superior delegation
  framework — detailed tool docs, aggressive delegation patterns,
  comprehensive MANDATORY delegation scenarios. This is the main agent
  prompt the user is interacting with now.
- =implementer.org= (386 lines) has implementation-specific sections
  (=* Writing Code=, =* Code quality=, =* Git workflow=, =* Output
  requirements=) that the coordinator lacks, but its delegation
  instructions are less comprehensive than the coordinator's.
- Key differences analyzed:

| Aspect                       | coordinator                                                                               | implementer                                                |
|------------------------------+-------------------------------------------------------------------------------------------+------------------------------------------------------------|
| Role                         | Orchestration                                                                             | Implementation                                             |
| Model                        | DeepSeek v4 Pro                                                                           | Claude Opus (overridden to DeepSeek by gptel-agent preset) |
| Delegates to researcher-deep | Yes                                                                                       | Yes                                                        |
| Delegates to implementer     | Yes                                                                                       | No (self)                                                  |
| Full tool docs               | Yes (ReadOrgLink, Glob, Grep, Read, Insert, Edit, Write, Bash, Eval, WebSearch, WebFetch) | Abbreviated                                                |
| Code quality section         | No                                                                                        | Yes                                                        |
| Git workflow section         | No                                                                                        | Yes                                                        |
| Output requirements          | No                                                                                        | Yes                                                        |
| Writing Code section         | No                                                                                        | Yes                                                        |

- =implementer-complex= design: start from coordinator prompt, then:
  1. Change role from orchestration to implementation
  2. Remove =researcher-deep= delegation (coordinator-level analysis stays
     with main agent; implementer-complex should just implement)
  3. Add =implementer-complex= to "never delegate" list (can't delegate to self)
  4. Keep =implementer= delegation (different agent, uses DeepSeek v4 Pro)
  5. Keep all other delegation (gatherer, researcher, executor, verifier, etc.)
  6. Merge implementation-specific sections from current =implementer.org=
     (=* Writing Code=, =* Code quality=, =* Git workflow=, =* Output requirements=)
  7. Use Claude Opus model, no model override in gptel-agent preset
  8. State words: =IMPLEMENT-COMPLEX IMPLEMENTING-COMPLEX IMPLEMENTED-COMPLEX=
  9. Add =implementer-complex= to the =Agent= tool's =:enum= in
     =gptel-agent-tools.el= line 2485
  10. Register in all agent presets that list subagent-models
      (=gptel-agent=, =gptel-deepseek=, =gptel-smart=, =gptel-coordinator=,
      =gptel-triage=, =gptel-dummy=)

**** Implementation plan (separate implementer dispatches)
1. Create =agents/implementer-complex.org= — start from coordinator.org,
   apply all modifications above.
2. Update =gptel-agent-tools.el= — add =implementer-complex= to the
   =Agent= tool enum.
3. Update all agent presets' =:subagent-models:= to include
   =:implementer-complex "Claude/claude-opus-4-7"=.
4. Update the agent definition table in =gptel-agent-ai.org=.
5. Update the state triads table in =gptel-agent-ai.org=.
6. Commit with message: "Add implementer-complex agent (Opus, coordinator-based)"

**** Decisions needed from user
- Confirm the approach: coordinator-based with implementation sections merged in
- Confirm researcher-deep should NOT be available to implementer-complex
- Confirm implementer-complex SHOULD be able to delegate to implementer
- Confirm model: Claude Opus 4.7 (=claude-opus-4-7=)
- Confirm state words: =IMPLEMENT-COMPLEX / IMPLEMENTING-COMPLEX / IMPLEMENTED-COMPLEX=

**** Outcomes / Results
- Created =agents/implementer-complex.org= (533 lines) — derived from coordinator prompt with implementation-specific sections merged
- Uses Claude Opus 4.7, state words: =IMPLEMENT-COMPLEX / IMPLEMENTING-COMPLEX / IMPLEMENTED-COMPLEX=
- Can delegate to =implementer=; cannot delegate to itself or =researcher-deep=
- Added to Agent tool enum in =gptel-agent-tools.el=
- Registered =:implementer-complex "Claude/claude-opus-4-7"= in all 6 agent presets (gptel-agent, gptel-deepseek, gptel-smart, gptel-coordinator, gptel-triage, gptel-dummy)
- Updated =gptel-agent-ai.org=: agent definition table + state triads table
- All 42 tests pass
- Commit: 610323f
